import styled from 'styled-components'

const Wrapper = styled.div`
  position: relative;
  width: 100%;
  height: 100%;
  display: grid;
  background: ${p => p.theme.colors.dark};
`

export { Wrapper }
